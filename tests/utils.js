const identity = (x) => x;
const double = (el) => 2 * el;
const triple = (el) => 3 * el;
const half = (x) => x / 2;
const tenTimes = (x) => 10 * x;

module.exports = {
  identity,
  double,
  triple,
  half,
  tenTimes,
};
